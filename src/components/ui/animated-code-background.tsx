/** Decorative code/grid overlay disabled — caused hydration mismatch and grid lines. */
export function AnimatedCodeBackground(_props: {
  variant?: string;
  className?: string;
  density?: "low" | "medium" | "high";
}) {
  return null;
}
