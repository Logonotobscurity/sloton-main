/**
 * Former full-screen 800ms overlay (AUD-UI-001). Kept as a no-op so layout
 * imports stay valid. Next.js streaming + `app/loading.tsx` handle wait states.
 */
export function WebsiteLoader() {
  return null;
}
